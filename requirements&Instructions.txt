//Mask_rcnn_mask: combined object detection and monodepth2  in order to run we need to change the path of the image we want to run 
to run Mask_rcnn_mask  use the command python mask_rcnn_mask.py --mask-rcnn mask-rcnn-coco --image images/example_01.jpg
//monodepth requirements
conda install pytorch=0.4.1 torchvision=0.2.1 -c pytorch
pip install tensorboardX==1.4
//octomap 
Installation: Dependency: opencv - sudo apt-get install libopencv-dev boost - sudo apt-get install libboost-all-dev pcl 
